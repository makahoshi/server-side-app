import React from 'react';

const loginPage = () => {
    return (
        <div>
            <h1>Please Login</h1>
            <button onClick={() => console.log('hi there!')}>Press Me</button>

        </div>

    )};

export default {
  component: loginPage
};