import React from 'react';

const admin = () => {
    return (
        <div>
            <h1>These are the admins of the site</h1>
            <button onClick={() => console.log('hi there!')}>Press Me</button>

        </div>

    )};

export default admin;