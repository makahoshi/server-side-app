import React from 'react';

const home = () => {
    return (
        <div>
        <h1>Welcome Makaila!!</h1>
            <button onClick={() => console.log('hi there!')}>Press Me</button>

        </div>

)}

export default home;